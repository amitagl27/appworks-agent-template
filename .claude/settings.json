{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "deny": [
      "Edit(**/*.cws)",
      "Write(**/*.cws)",
      "Edit(**/.identifiers)",
      "Write(**/.identifiers)",
      "Edit(**/*Base.java)",
      "Write(**/*Base.java)",
      "Edit(**/*#cws-ma#.cws/**)",
      "Write(**/*#cws-ma#.cws/**)",
      "Edit(**/*#ef#/**)",
      "Write(**/*#ef#/**)",
      "Bash(git config user.email *)",
      "Bash(git commit --no-verify *)",
      "Bash(git push --force *)",
      "Bash(git push -f *)"
    ]
  },
  "_comment": "These deny rules implement the protections documented in CLAUDE.md. Do not relax them without updating CLAUDE.md and the pre-commit hook in lockstep. The Bash denies prevent an agent from impersonating the AppWorks sync identity or bypassing the pre-commit hook."
}
