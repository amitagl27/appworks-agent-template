# Local archive of original test/reference material — never publish to the template
docs/archive/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# Secrets — defense in depth (LinkedIn article calls this out explicitly)
.env
.env.*
*.pem
*.key
secrets/
credentials.json

# Local AppWorks/CWS scratch (per-developer caches, not workspace state)
.cws-cache/
.cws-temp/

# Build outputs from any sibling code modules
target/
build/
dist/
node_modules/
*.class
